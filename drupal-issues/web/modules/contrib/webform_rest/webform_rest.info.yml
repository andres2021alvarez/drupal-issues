name: 'Webform REST'
type: module
description: 'Allows retrieving webform elements and submitting webforms via REST'
package: 'Webform'
core_version_requirement: ^10.3 || ^11
configure: webform_rest.settings
dependencies:
  - webform:webform
  - drupal:rest
